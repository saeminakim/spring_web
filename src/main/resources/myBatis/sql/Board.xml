<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mvc.repository.BoardRepository">
	<select id="getList" parameterType="com.example.mvc.parameter.BoardSearchParameter" resultType="com.example.mvc.domain.Board">
		SELECT
		B.BOARD_SEQ,
		B.TITLE,
		B.CONTENTS,
		B.REG_DATE
		FROM BOARD B
		<where>
			<if test="@org.apache.commons.lang3.StringUtils@isNotEmpty(keyword)">
			AND B.TITLE LIKE CONCAT('%', #{keyword}, '%')
			</if>
		</where>
		ORDER BY B.REG_DATE DESC
	</select>
	
	<select id="get" parameterType="int" resultType="com.example.mvc.domain.Board">
		SELECT
		B.BOARD_SEQ,
		B.TITLE,
		B.CONTENTS,
		B.REG_DATE
		FROM BOARD B
		WHERE B.BOARD_SEQ = #{boardSeq}
	</select>
	
	<insert id="save" parameterType="com.example.mvc.parameter.BoardParameter" useGeneratedKeys="true" keyProperty="boardSeq">
		INSERT INTO BOARD
		(
		 	TITLE,
			CONTENTS,
			REG_DATE
		)
		VALUES
		(
			#{title},
			#{contents},
			NOW()
		)
	</insert>
	
	<update id="update" parameterType="com.example.mvc.domain.Board">
		UPDATE BOARD
		SET
			TITLE = #{title},
			CONTENTS = #{contents}
		WHERE BOARD_SEQ = #{boardSeq}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM BOARD
		WHERE BOARD_SEQ = #{boardSeq}
	</delete>
	
	<insert id="saveList" parameterType="map">
		INSERT INTO BOARD
		(
			TITLE,
			CONTENTS,
			REG_DATE
		)
		VALUES
		<foreach collection="boardList" item="board" separator=",">
		(
			#{board.title},
			#{board.contents},
			NOW()
		)	
		</foreach>
		
	</insert>
	
</mapper>